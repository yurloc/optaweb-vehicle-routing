// @flow

export type LocationType = {
  id: number,
  lat: number,
  lng: number,
}

export type LocationHandler = (
  id: number
) => void

export type ClickButtonHandler = (
  e: SyntheticEvent<HTMLButtonElement>
) => void
